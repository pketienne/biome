# should not generate diagnostics
# camelCase anchors
defaults: &myDefaults
  timeout: 30
production:
  <<: *myDefaults

# single word
base: &config
  host: localhost
ref: *config

# camelCase with numbers
v2: &apiV2Settings
  version: 2
use: *apiV2Settings
