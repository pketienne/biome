# Simple duplicate
first: &anchor value1
second: &anchor value2

# Triple duplicate
a: &dup one
b: &dup two
c: &dup three

# Nested duplicate anchors
outer:
  inner1: &nested val1
  inner2: &nested val2

# Flow context duplicate
flow1: &flow_dup {key: 1}
flow2: &flow_dup [a, b]
